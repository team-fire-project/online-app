<section class="form-container">
  <div class="add-item-box">
    <h2>Edit Inventory Item</h2>
    <div class="formBox">
      <form onsubmit="editItem({{inventory.id}})">
        {{! Item Name }}
        <div class="inputBox">
          <span><label for="name">Item Name</label></span>
          <input
            type="text"
            id="edit-form-inventory-name"
            name="name"
            placeholder="{{inventory.name}}"
            value="{{inventory.name}}"
            required
          />

        </div>
        {{! Category }}
        <div class="inputBox">
          <span><label for="category">Category</label></span>
          <select name="category" id="edit-form-inventory-category" required>
            <option value="">--Please choose a category--</option>
            <option value="electronics">Electronics</option>
            <option value="jewelery">Jewelery</option>
            <option value="men's clothing">Men's Clothing</option>
            <option value="women's clothing">Women's Clothing</option>
          </select>
        </div>
        {{! Item Price }}
        <div class="inputBox">
          <span><label for="price">Item Price</label></span>
          <input
            type="number"
            id="edit-form-inventory-price"
            name="price"
            value="{{inventory.price}}"
            required
          />
        </div>
        {{! Inventory Count }}
        <div class="inputBox" id="counts-imageUrl">
          <div id="edit-countsBox">
            <span><label for="counts">Inventory Count</label></span>
            <input
              type="number"
              id="edit-form-inventory-count"
              name="counts"
              value="{{inventory.counts}}"
              required
            />
          </div>
          <div id="edit-imageUrlBox">
            <span><label for="image">Image URL</label></span>
            <input
              type="url"
              id="edit-form-inventory-image"
              name="image"
              value="{{inventory.image}}"
              required
            />
          </div>

        </div>
        {{! Image URL }}
        <div class="inputBox">

        </div>
        {{! Description }}
        <div class="inputBox">
          <span><label for="description">Description</label></span>

          <input
            type="text"
            id="edit-form-inventory-description"
            name="description"
            value="{{inventory.description}}"
            required
          />
        </div>

        {{! Buttons }}
        <div class="inputBox" id="form-cta">
          <button class="modal-button"><a
              class=""
              href="/stockhome/inventories/"
            >Cancel</a>
          </button>
          <button
            class="modal-confirm"
            id="edit-submit"
            type="submit"
          >Submit</button>
        </div>
      </form>
    </div>
  </div>
  <script src="/add-inventory-form.js"></script>
</section>

<script src="/edit-inventory-form.js"></script>